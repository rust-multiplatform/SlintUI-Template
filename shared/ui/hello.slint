export HelloWorld := Window {
    preferred-width: 640px;
    preferred-height: 480px;

    Rectangle {
        width: 640px;
        height: 480px;
        background: cyan;
    }

    Text {
        x: 300px; // Roughly centered
        y: 200px; // Roughly centered
        text: "Hello, World!";
    }
}
